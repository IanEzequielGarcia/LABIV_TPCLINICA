import { BotonGorditoDirective } from './boton-gordito.directive';

describe('BotonGorditoDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonGorditoDirective();
    expect(directive).toBeTruthy();
  });
});
