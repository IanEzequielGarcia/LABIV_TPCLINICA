import { ElementoNocheDirective } from './elemento-noche.directive';

describe('ElementoNocheDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementoNocheDirective();
    expect(directive).toBeTruthy();
  });
});
