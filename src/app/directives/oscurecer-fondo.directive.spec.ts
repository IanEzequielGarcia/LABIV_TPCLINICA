import { OscurecerFondoDirective } from './oscurecer-fondo.directive';

describe('OscurecerFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new OscurecerFondoDirective();
    expect(directive).toBeTruthy();
  });
});
