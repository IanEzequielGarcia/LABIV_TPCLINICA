import { PacientePipePipe } from './paciente-pipe.pipe';

describe('PacientePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PacientePipePipe();
    expect(pipe).toBeTruthy();
  });
});
